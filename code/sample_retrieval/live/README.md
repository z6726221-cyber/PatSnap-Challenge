# live 检索目录

这个目录是黑盒检索模块的实时输出目录。

约定：

- 路径固定为 `code/sample_retrieval/live/`。
- 每次真实检索可以覆盖本目录下的 `.md` 文件。
- 后端真实首页链路会读取这里的 `.md`，并把用户当次问题作为 `Case.question`。
- 真实召回内容可能包含内部资料，不提交到 Git。

本目录只提交 `.gitkeep` 和这个说明文件，用于保留目录结构。
